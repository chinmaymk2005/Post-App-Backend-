<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-5">

        <div class="flex w-full justify-end">
            <a class="bg-red-500 p-2 text-sm rounded-md" href="/logout">Log-Out</a>
        </div>
                 
        <h5 class="mb-5">edit your post</h5> 

        <form action="/update/<%= post._id %>" method="post">   
            <textarea placeholder="What's on your mind"
                class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"><%= post.content %></textarea> 
            <input class="bg-yellow-500 p-1 rounded-md font-serif block mt-3 w-40 text-black" type="submit"
                value="Update Post !">  
        </form>

       
</body>

</html>
