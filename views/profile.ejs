<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Profile</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>

    <div class="w-full min-h-screen bg-zinc-900 text-white p-5">

        <div class="flex w-full justify-end">
            <a class="bg-red-500 p-2 text-sm rounded-md" href="/logout">Log-Out</a>
        </div>

        <h3 class="text-3xl mb-5 mt-5">Hello , <%= user.name %>👋</h3>
        <h5 class="mb-5">You can create a new post</h5>

        <form action="/post" method="post"> 
            <textarea placeholder="What's on your mind"
                class="block p-3 outline-none w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md"
                name="content"></textarea> 
            <input class="bg-green-500 p-1 rounded-md font-serif block mt-3 w-40" type="submit"
                value="Create new Post !!">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Post</h3>

            <div class="postcontainer mt-3"> 
                 <% user.posts.reverse().forEach(function(post){ %> 
                    <div class= "rounded-md post w-1/3 p-4 border-[1px] mb-3 border-zinc-600">
                        <h4 class="text-blue-500">@<%= user.username %></h4> <br>
                        <p class="text-sm tracking-tight text-white"><span><%= post.content %></span></p>     

                        <% if(post.likes.length == 0){ %> 
                            <div class="btns flex mt-1 gap-3">  
                                <a class="text-blue-500" href="/like/<%= post._id %>">Like</a>
                                <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a> 
                            </div>
                        <% } else if(post.likes.length > 0){ %>                          
                            <small class="inline-block mt-2"><%= post.likes.length %>❤️</small>   
                            <div class="btns flex mt-1 gap-3">  
                                <a class="text-blue-500" href="/like/<%= post._id %>">Unlike</a>
                                <a class="text-zinc-400" href="/edit/<%= post._id %>">Edit</a> 
                            </div>     
                        <% } %>                             
                        
                    </div>     
                 <% }) %>                  
                                 
            </div>   

        </div>
    </div>

</body>

</html>